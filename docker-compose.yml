services:
  hive:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VERSION: ${VERSION:-dev}
    ports:
      - "3000:8080"
    volumes:
      - ./config:/config
    env_file:
      - .env
    restart: unless-stopped
